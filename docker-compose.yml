version: '3.8'

services:
  myledger-api:
    build: .
    container_name: myledger-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - TZ=Asia/Kolkata