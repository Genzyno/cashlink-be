version: '3.8'

services:
  myledger-api:
    build: .
    container_name: myledger-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - /home/ubuntu/config/myledger.env
